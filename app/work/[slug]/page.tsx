import { notFound } from "next/navigation";import { projects } from "@/components/work/WorkSection";
export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)notFound();return <main className="main"><section className="simple-page"><div className="content simple-layout"><div className="section-label">Project / {p.year}</div><h1 className="simple-title">{p.client}</h1><div className="simple-copy"><p>{p.category}</p><a className="btn btn--primary" href={p.url} target="_blank" rel="noreferrer">VISIT PROJECT ↗</a></div></div></section></main>}
